machine:
  node:
    version: 6.9.1

dependencies:
  pre:
    - npm install -g typescript 
    - npm install -g mocha 

test:
  pre:
    - npm build

general:
  branches:
    ignore:
      - gh-pages
